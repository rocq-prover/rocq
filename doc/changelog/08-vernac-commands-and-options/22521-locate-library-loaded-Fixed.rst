- **Fixed:**
  :cmd:`Locate Library` said a library had been loaded when it was only
  bound to a file, and extraction did not ask for such a library to be
  loaded first
  (`#22521 <https://github.com/rocq-prover/rocq/pull/22521>`_,
  by remix7531).
